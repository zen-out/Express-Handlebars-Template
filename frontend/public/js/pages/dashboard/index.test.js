describe("lets begin with a couple of requirements", () => {
    it("should be able to get the id attributes upon submission", () => {

    })

    it("should be able to return an array of objects upon submission", () => {

    })

    it("should be able to display the array of objects, updated, upon submission", () => {

    })

    it("should be able to pass in the array of objects into the handlebar template", () => {

    })

    it("the data attributes for each of the handlebars template should be there", () => {})
})