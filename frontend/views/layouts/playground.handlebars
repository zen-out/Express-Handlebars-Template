
<!doctype html>
<html>
    <head>
        <meta charset="utf-8" />
    </head>
    <body>
        <h1>yooo!</h1>
        {{> page/playground/playground}}

        {{{body}}}

        {{! Only needs the Handlebars Runtime, because the
        exposed templates are precompiled. }}
        <script
            src="//cdn.jsdelivr.net/handlebarsjs/4.0.5/handlebars.runtime.min.js"></script>
        <script>
        (function () {
            var revive    = Handlebars.template,
                templates = Handlebars.templates = Handlebars.templates || {};

          {{#templates}}
            templates['{{{name}}}'] = revive({{{template}}});
          {{/templates}}
        }());
    </script>
    </body>
</html>
